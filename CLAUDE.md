# CLAUDE.md

This project's rules for AI coding agents live in **[AGENTS.md](AGENTS.md)** — the
single source of truth. Read it first.

Per-directory rules live in the `AGENTS.md` file inside each box
(`data_prep/`, `model_library/`, `strategy_testing/`, `risk_management/`,
`live_paper_execution/`, `interface/`, `horizontal_foundation/`).

Do not duplicate guidance here; update `AGENTS.md` instead.
